use std::simd;
use std::ptr;
use std::slice;

unsafe fn encode_u8_0_8(_: *const u8, _: *mut u32) { }
unsafe fn encode_u8_0_16(_: *const u8, _: *mut u32) { }
unsafe fn encode_u8_0_24(_: *const u8, _: *mut u32) { }
unsafe fn encode_u8_0_32(_: *const u8, _: *mut u32) { }
unsafe fn encode_u8_1_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    *s_ptr = out;
}
unsafe fn encode_u8_1_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    *s_ptr = out;
}
unsafe fn encode_u8_1_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    *s_ptr = out;
}
unsafe fn encode_u8_1_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 29usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
}
unsafe fn encode_u8_2_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    *s_ptr = out;
}
unsafe fn encode_u8_2_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
}
unsafe fn encode_u8_2_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    *s_ptr = out;
}
unsafe fn encode_u8_2_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
}
unsafe fn encode_u8_3_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    *s_ptr = out;
}
unsafe fn encode_u8_3_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    *s_ptr = out;
}
unsafe fn encode_u8_3_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    *s_ptr = out;
}
unsafe fn encode_u8_3_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 29usize;
    *s_ptr = out;
}
unsafe fn encode_u8_4_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
}
unsafe fn encode_u8_4_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
}
unsafe fn encode_u8_4_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
}
unsafe fn encode_u8_4_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
}
unsafe fn encode_u8_5_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    *s_ptr = out;
}
unsafe fn encode_u8_5_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    *s_ptr = out;
}
unsafe fn encode_u8_5_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    *s_ptr = out;
}
unsafe fn encode_u8_5_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 29usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 3usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    *s_ptr = out;
}
unsafe fn encode_u8_6_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    *s_ptr = out;
}
unsafe fn encode_u8_6_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    *s_ptr = out;
}
unsafe fn encode_u8_6_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    *s_ptr = out;
}
unsafe fn encode_u8_6_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    *s_ptr = out;
}
unsafe fn encode_u8_7_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    *s_ptr = out;
}
unsafe fn encode_u8_7_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 5usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    *s_ptr = out;
}
unsafe fn encode_u8_7_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 5usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 6usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    *s_ptr = out;
}
unsafe fn encode_u8_7_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 7usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 14usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 21usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 28usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 4usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 3usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 10usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 17usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 31usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 1usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 6usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 13usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 20usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 27usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 5usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 2usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 9usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 23usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 30usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 2usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 5usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 12usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 19usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 26usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 6usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 1usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 15usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 22usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 29usize;
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = (*i_ptr >> 3usize) as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 4usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 11usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 18usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 25usize;
    *s_ptr = out;
}
unsafe fn encode_u8_8_8(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    *s_ptr = out;
}
unsafe fn encode_u8_8_16(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    *s_ptr = out;
}
unsafe fn encode_u8_8_24(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    *s_ptr = out;
}
unsafe fn encode_u8_8_32(mut i_ptr: *const u8, mut s_ptr: *mut u32) {
    let mut out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    i_ptr = i_ptr.offset(1);
    *s_ptr = out;
    s_ptr = s_ptr.offset(1);
    out = *i_ptr as u32;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 8usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 16usize;
    i_ptr = i_ptr.offset(1);
    out |= (*i_ptr as u32) << 24usize;
    *s_ptr = out;
}
pub const ENCODE_U8: [[unsafe fn(*const u8, *mut u32); 4usize]; 9usize] =
    [[encode_u8_0_8, encode_u8_0_16, encode_u8_0_24, encode_u8_0_32],
        [encode_u8_1_8, encode_u8_1_16, encode_u8_1_24, encode_u8_1_32],
        [encode_u8_2_8, encode_u8_2_16, encode_u8_2_24, encode_u8_2_32],
        [encode_u8_3_8, encode_u8_3_16, encode_u8_3_24, encode_u8_3_32],
        [encode_u8_4_8, encode_u8_4_16, encode_u8_4_24, encode_u8_4_32],
        [encode_u8_5_8, encode_u8_5_16, encode_u8_5_24, encode_u8_5_32],
        [encode_u8_6_8, encode_u8_6_16, encode_u8_6_24, encode_u8_6_32],
        [encode_u8_7_8, encode_u8_7_16, encode_u8_7_24, encode_u8_7_32],
        [encode_u8_8_8, encode_u8_8_16, encode_u8_8_24, encode_u8_8_32]];
unsafe fn decode_u8_0_8(_: *const u32, o_ptr: *mut u8) {
    ptr::write_bytes(o_ptr, 0u8, 8usize);
}
unsafe fn decode_u8_0_16(_: *const u32, o_ptr: *mut u8) {
    ptr::write_bytes(o_ptr, 0u8, 16usize);
}
unsafe fn decode_u8_0_24(_: *const u32, o_ptr: *mut u8) {
    ptr::write_bytes(o_ptr, 0u8, 24usize);
}
unsafe fn decode_u8_0_32(_: *const u32, o_ptr: *mut u8) {
    ptr::write_bytes(o_ptr, 0u8, 32usize);
}
unsafe fn decode_u8_1_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 7usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_1_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 7usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_1_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 7usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_1_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 7usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 29usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_2_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 6usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_2_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 6usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_2_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 6usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_2_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 6usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_3_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 5usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_3_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 5usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_3_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 5usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_3_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 5usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 29usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_4_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 4usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_4_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 4usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_4_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 4usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_4_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 4usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_5_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 3usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_5_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 3usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_5_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 3usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_5_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 3usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 29usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 3usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_6_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 2usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_6_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 2usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_6_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 2usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_6_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 2usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_7_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 1usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_7_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 1usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 5usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_7_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 1usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 5usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 6usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_7_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mask: u8 = !0 >> 1usize;
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 7usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 14usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 21usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 28usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 4usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 3usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 10usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 17usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 31usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 1usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 6usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 13usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 20usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 27usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 5usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 2usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 9usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 23usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 30usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 2usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 5usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 12usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 19usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 26usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 6usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 1usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 15usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 22usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 29usize) as u8;
    s_ptr = s_ptr.offset(1);
    out |= (*s_ptr as u8) << 3usize;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 4usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 11usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 18usize) as u8;
    *o_ptr = out & mask;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 25usize) as u8;
    *o_ptr = out & mask;
}
unsafe fn decode_u8_8_8(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
}
unsafe fn decode_u8_8_16(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
}
unsafe fn decode_u8_8_24(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
}
unsafe fn decode_u8_8_32(mut s_ptr: *const u32, mut o_ptr: *mut u8) {
    let mut out;
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    s_ptr = s_ptr.offset(1);
    out = *s_ptr as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 8usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 16usize) as u8;
    *o_ptr = out;
    o_ptr = o_ptr.offset(1);
    out = (*s_ptr >> 24usize) as u8;
    *o_ptr = out;
}
pub const DECODE_U8: [[unsafe fn(*const u32, *mut u8); 4usize]; 9usize] =
    [[decode_u8_0_8, decode_u8_0_16, decode_u8_0_24, decode_u8_0_32],
        [decode_u8_1_8, decode_u8_1_16, decode_u8_1_24, decode_u8_1_32],
        [decode_u8_2_8, decode_u8_2_16, decode_u8_2_24, decode_u8_2_32],
        [decode_u8_3_8, decode_u8_3_16, decode_u8_3_24, decode_u8_3_32],
        [decode_u8_4_8, decode_u8_4_16, decode_u8_4_24, decode_u8_4_32],
        [decode_u8_5_8, decode_u8_5_16, decode_u8_5_24, decode_u8_5_32],
        [decode_u8_6_8, decode_u8_6_16, decode_u8_6_24, decode_u8_6_32],
        [decode_u8_7_8, decode_u8_7_16, decode_u8_7_24, decode_u8_7_32],
        [decode_u8_8_8, decode_u8_8_16, decode_u8_8_24, decode_u8_8_32]];
unsafe fn encode_simd_u8_0(_: *const u8, _: *mut u32) { }
unsafe fn encode_simd_u8_1(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 16usize);
    let rhs =


        simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 1usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 3usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 5usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 6usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 7usize;
    lhs.store(s_slice, 0usize);
}
unsafe fn encode_simd_u8_2(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 32usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 6usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs.store(s_slice, 0usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 16usize);
}
unsafe fn encode_simd_u8_3(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 48usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 3usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 0usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 1usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 7usize;
    lhs.store(s_slice, 16usize);
    lhs = rhs >> 1usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 5usize;
    lhs.store(s_slice, 32usize);
}
unsafe fn encode_simd_u8_4(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 64usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs.store(s_slice, 0usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs.store(s_slice, 16usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 4usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs.store(s_slice, 32usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 4usize;
    lhs.store(s_slice, 48usize);
}
unsafe fn encode_simd_u8_5(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 80usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 5usize;
    lhs.store(s_slice, 0usize);
    lhs = rhs >> 3usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 7usize;
    lhs.store(s_slice, 16usize);
    lhs = rhs >> 1usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs = lhs | rhs << 4usize;
    lhs.store(s_slice, 32usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 1usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 48usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 3usize;
    lhs.store(s_slice, 64usize);
}
unsafe fn encode_simd_u8_6(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 96usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 0usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 4usize;
    lhs.store(s_slice, 16usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 2usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs.store(s_slice, 32usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 48usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 4usize;
    lhs.store(s_slice, 64usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 2usize;
    lhs.store(s_slice, 80usize);
}
unsafe fn encode_simd_u8_7(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 112usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs = lhs | rhs << 7usize;
    lhs.store(s_slice, 0usize);
    lhs = rhs >> 1usize;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs = lhs | rhs << 6usize;
    lhs.store(s_slice, 16usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs = lhs | rhs << 5usize;
    lhs.store(s_slice, 32usize);
    lhs = rhs >> 3usize;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs = lhs | rhs << 4usize;
    lhs.store(s_slice, 48usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs = lhs | rhs << 3usize;
    lhs.store(s_slice, 64usize);
    lhs = rhs >> 5usize;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs = lhs | rhs << 2usize;
    lhs.store(s_slice, 80usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs = lhs | rhs << 1usize;
    lhs.store(s_slice, 96usize);
}
unsafe fn encode_simd_u8_8(i_ptr: *const u8, s_ptr: *mut u32) {
    let i_slice = slice::from_raw_parts(i_ptr, 128);
    let s_slice = slice::from_raw_parts_mut(s_ptr as *mut u8, 128usize);
    let rhs = simd::u8x16::load(i_slice, 0usize);
    let mut lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 16usize);
    lhs.store(s_slice, 0usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 32usize);
    lhs.store(s_slice, 16usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 48usize);
    lhs.store(s_slice, 32usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 64usize);
    lhs.store(s_slice, 48usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 80usize);
    lhs.store(s_slice, 64usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 96usize);
    lhs.store(s_slice, 80usize);
    lhs = rhs;
    let rhs = simd::u8x16::load(i_slice, 112usize);
    lhs.store(s_slice, 96usize);
    lhs = rhs;
    lhs.store(s_slice, 112usize);
}
pub const ENCODE_SIMD_U8: [unsafe fn(*const u8, *mut u32); 9usize] =
    [encode_simd_u8_0, encode_simd_u8_1, encode_simd_u8_2, encode_simd_u8_3,
        encode_simd_u8_4, encode_simd_u8_5, encode_simd_u8_6, encode_simd_u8_7,
        encode_simd_u8_8];
unsafe fn decode_simd_u8_0(_: *const u32, o_ptr: *mut u8) {
    ptr::write_bytes(o_ptr, 0u8, 128);
}
unsafe fn decode_simd_u8_1(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 16usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 7usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 3usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 5usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 6usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 7usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_2(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 32usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 6usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 6usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_3(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 48usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 5usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 3usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = lhs | rhs << 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 7usize;
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = lhs | rhs << 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 5usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_4(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 64usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 4usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    let rhs = simd::u8x16::load(s_slice, 48usize);
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_5(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 80usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 3usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 5usize;
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = lhs | rhs << 3usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 7usize;
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = lhs | rhs << 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(s_slice, 48usize);
    lhs = lhs | rhs << 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 64usize);
    lhs = lhs | rhs << 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 3usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_6(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 96usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 2usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = lhs | rhs << 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = lhs | rhs << 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    let rhs = simd::u8x16::load(s_slice, 48usize);
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 64usize);
    lhs = lhs | rhs << 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(s_slice, 80usize);
    lhs = lhs | rhs << 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_7(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 112usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let mask = simd::u8x16::splat(!0) >> 1usize;
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs = lhs & mask;
    lhs.store(o_slice, 0usize);
    lhs = rhs >> 7usize;
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = lhs | rhs << 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 16usize);
    lhs = rhs >> 6usize;
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = lhs | rhs << 2usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 32usize);
    lhs = rhs >> 5usize;
    let rhs = simd::u8x16::load(s_slice, 48usize);
    lhs = lhs | rhs << 3usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 48usize);
    lhs = rhs >> 4usize;
    let rhs = simd::u8x16::load(s_slice, 64usize);
    lhs = lhs | rhs << 4usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 64usize);
    lhs = rhs >> 3usize;
    let rhs = simd::u8x16::load(s_slice, 80usize);
    lhs = lhs | rhs << 5usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 80usize);
    lhs = rhs >> 2usize;
    let rhs = simd::u8x16::load(s_slice, 96usize);
    lhs = lhs | rhs << 6usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 96usize);
    lhs = rhs >> 1usize;
    lhs = lhs & mask;
    lhs.store(o_slice, 112usize);
}
unsafe fn decode_simd_u8_8(s_ptr: *const u32, o_ptr: *mut u8) {
    let s_slice = slice::from_raw_parts(s_ptr as *const u8, 128usize);
    let o_slice = slice::from_raw_parts_mut(o_ptr, 128);
    let rhs = simd::u8x16::load(s_slice, 0usize);
    let mut lhs;
    lhs = rhs;
    lhs.store(o_slice, 0usize);
    let rhs = simd::u8x16::load(s_slice, 16usize);
    lhs = rhs;
    lhs.store(o_slice, 16usize);
    let rhs = simd::u8x16::load(s_slice, 32usize);
    lhs = rhs;
    lhs.store(o_slice, 32usize);
    let rhs = simd::u8x16::load(s_slice, 48usize);
    lhs = rhs;
    lhs.store(o_slice, 48usize);
    let rhs = simd::u8x16::load(s_slice, 64usize);
    lhs = rhs;
    lhs.store(o_slice, 64usize);
    let rhs = simd::u8x16::load(s_slice, 80usize);
    lhs = rhs;
    lhs.store(o_slice, 80usize);
    let rhs = simd::u8x16::load(s_slice, 96usize);
    lhs = rhs;
    lhs.store(o_slice, 96usize);
    let rhs = simd::u8x16::load(s_slice, 112usize);
    lhs = rhs;
    lhs.store(o_slice, 112usize);
}
pub const DECODE_SIMD_U8: [unsafe fn(*const u32, *mut u8); 9usize] =
    [decode_simd_u8_0, decode_simd_u8_1, decode_simd_u8_2, decode_simd_u8_3,
        decode_simd_u8_4, decode_simd_u8_5, decode_simd_u8_6, decode_simd_u8_7,
        decode_simd_u8_8];
pub unsafe fn encode_delta_u8(i_ptr: *mut u8, length: usize) {
    let i_slice = slice::from_raw_parts_mut(i_ptr as *mut i8, length);
    let mut idx = length;
    for _ in 0..((idx - 1) / 16usize) {
        idx -= 16usize;
        let lhs =
            simd::i8x16::load(i_slice, idx) -
                simd::i8x16::load(i_slice, idx - 1);
        lhs.store(i_slice, idx);
    }
    for a in (1..(idx as isize)).rev() {
        let lwr = *i_ptr.offset(a - 1);
        *i_ptr.offset(a) = (*i_ptr.offset(a)).wrapping_sub(lwr);
    }
}
pub unsafe fn encode_zz_shift_u8(i_ptr: *mut u8, length: usize, shift: u8) {
    let i_ptr: *mut i8 = i_ptr as *mut i8;
    let shift: i8 = shift as i8;
    for a in 0..(length as isize) {
        let rhs = (*i_ptr.offset(a)).wrapping_sub(shift);
        *i_ptr.offset(a) = (rhs << 1) ^ (rhs >> 7usize);
    }
}
